[project]
name = "h2yaml"
version = "0.1"
requires-python = ">= 3.10"
dependencies = [
  "pyyaml",
  "libclang",
  "type_enforced"
]

[project.scripts]
h2yaml = "h2yaml:main"

[project.optional-dependencies]
test = [
  "pytest",
  "pytest-icdiff",
  "coverage"
]

[tool.setuptools]
py-modules = ["h2yaml"]
