[project]
name = "h2yaml"
version = "0.1"
requires-python = ">= 3.8"
dependencies = [
  "pyyaml",
  "libclang",
  "type_enforced"
]

[project.optional-dependencies]
test = [
  "pytest",
  "pytest-icdiff",
  "coverage"
]

[tool.setuptools]
py-modules = ["h2yaml"]
